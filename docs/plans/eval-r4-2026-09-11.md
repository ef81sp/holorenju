# eval-r4: 多様コーパスでの prospect 重み再学習 — 設計メモ v2

- 日付: 2026-09-11（v1 → v2: /review 3 観点の指摘を反映。変更点は §9）
- 位置づけ: `strength-screen-2026-09-08.md` §5（後段）。§3 の単セル摂動スクリーンは全消化（採用 1 本 = P1 プローブ上限半減 +27.6、eval 側は全部 ±10 以内で不採用）。
- 目的: hard の prospect 重み 34 セルを、**多様な開局・現行エンジン・序盤帯（ply 4〜6 の黒番/白番）込み**のコーパスで Rapfi 教師 Texel 再学習し、時間モードで **+10 以上（非劣性込み）** なら採用する。
- 判定を先に決める: 採否は **回帰ゲート → 固定スクリーン（棄却判定のみ）→ 時間ゲート** で決める。損失は診断専用で、採否・停止条件には使わない（§4、r2 の前例: 量子化後の損失は同点でも J6 反転＋部分ベンチ +56）。

## 0. 地形（再導出しない）

| 事実                                                                                                                                                                                                                                                                                                                                                                                                            | 出典                                   |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- |
| r2（2026-07-15）: 12,437 局面（うち序盤帯 624 行。抽出方法は p3 doc に記載なし）、s=3.619、28 回帰 + 6 アンカー。J6 回帰 PASS。r2 vs r1 は 220 局中断の部分結果 +56                                                                                                                                                                                                                                             | `prospect-texel-p3-2026-07-15.md` P6   |
| prospect 基底 vs legacy の +181 は **r1**（P3-d、Gate 2、8a752f4）の値                                                                                                                                                                                                                                                                                                                                          | 同 Gate 2                              |
| r3（2026-07-17）: 同一エンジン自己対局 +1,348 局面（ply<8 ゼロ）で fold val 0.085022→0.084995＝利得ゼロ、**J6 回帰 FAIL で棄却**                                                                                                                                                                                                                                                                                | `prospect-texel-r3-2026-07-17.md`      |
| r3 の教訓: (a) ply4-7 帯を必ず含める (b) J6 級回帰ゲートは採用前必須 (c) J6 はブック/回帰アンカーで固定するのが構造的に安全                                                                                                                                                                                                                                                                                     | 同 §教訓                               |
| 教師コーパス（14,608 行）は消失。中間 JSONL は gitignore                                                                                                                                                                                                                                                                                                                                                        | strength-screen §0（本日確認）         |
| 単セル摂動（S4/S4'、±100）は ±10 Elo 以内 → 重みは現コーパス上で局所最適。r4 の利得は「コーパスの分布シフト」からしか出ない                                                                                                                                                                                                                                                                                     | strength-screen §6.18                  |
| 利用可能な棋譜（`bench-results/{commit,weight}-bench-*.json`）: 53 本のうち games 欠落 4 本（2026-06-11 の weight-bench）と `valid:false` 1 本を除き **48 本・15,616 局**（2026-06: 3,848 / 08: 3,918 / 09: 7,850）。全ファイル `{games:[...]}` 形（トップレベル配列は存在しない。v1 の記述は誤り）。旧 `prospect-corpus.ts` が weight-bench を読めなかった理由は**ファイル名フィルタ**（`commit-bench-` のみ） | A1 イシューレビューで実ファイル確認    |
| 09 分は開局スイート v1/v2 起点（**全開局が 7 手**）で distinct 100%、エンジンは prospect+P1 前後                                                                                                                                                                                                                                                                                                                | strength-screen §6                     |
| `src/assets/opening-book-hard.json`: 5,951 エントリ（白番 5,950 + 黒番 1）、石数 **3: 34 / 5: 321 / 6: 1 / 7: 5,595**（94% が ply 7 白番）。key = `boardToString`（B/W/.）225 文字 + `\|side`、`parseBoardKey` で復元可。r2 重み世代（`weightGeneration: texel-r2-rapfi-v2`）で生成、Rapfi 誘導済み                                                                                                             | 本日集計、`opening-book-2026-07-16.md` |
| Rapfi: `tools/oracle/pbrain-rapfi-macos-apple-silicon`（起動確認済み）。ラベリング ≈ 55 局面/秒（4 並列、100 ms。r3 実測 57.4/s）                                                                                                                                                                                                                                                                               | `reference_rapfi_oracle`、r3 doc       |
| パイプライン: `prospect-corpus.ts`（quiet フィルタ・dedup）→ `rapfi/labelCorpus.ts`（resume 可）→ `prospect-texel.ts`（k-fold、rapfi/outcome 両教師、`groupKey = source.file#gameIdx`）→ `prospect-anchor.ts`（s 較正・min-support 100・アンカー 6 セル）→ prospect.zig `PROSPECT_SCORE_DEFAULT` 焼き込み（TS 側に既定値表なし = SSoT は wasm、T1）                                                             | p3 runbook §再現手順                   |
| 抽出の実測（perf レビュー、300 局サンプル）: **9.4 局面/局**（dedup 29%）、抽出 11 s/300 局、ply 分布 4-7: 537 / 8-15: 1,528 / 16-25: 688 / 26-35: 52 / >35: 0（`--max-per-game=12` × 間隔 1 で枠が尽きる）                                                                                                                                                                                                     | perf レビュー                          |
| `prospect-texel.ts` の fit: 合成 5 万行 × 34 で 3,234 反復 21 s。10 万行 ≈ 45 s/fit、`--teacher=both --k=5` = 12 fit ≈ 5〜10 分                                                                                                                                                                                                                                                                                 | perf レビュー実測                      |
| 手調整の脅威状態テーブル加算は −124 Elo（二重計上）。テーブルは基底の一部として Texel 適合前提でのみ意味がある                                                                                                                                                                                                                                                                                                  | `eval-basis-prospect-2026-07-13.md:29` |
| 時間モードの hard は 2026-09-09 以降プローブ 25 ms（#163）。ゲートは新 hard 基準で行う                                                                                                                                                                                                                                                                                                                          | strength-screen §6.4                   |

## 1. コーパス設計

| 源              | 内容                                                                                                                                                          | 規模見込み                                         | 備考                                                                                                                                                                                            |
| --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 棋譜            | 53 JSON・15,616 局。`--min-ply=4 --sample-interval=1 --max-per-game=24`（事前登録フィルタの変更: 上限 12→24。距離 1・上限 12 だと ply 26 以降が採れないため） | 8〜12 万局面（ファイル横断 dedup 後）              | 入力 prefix に `weight-bench-` を追加。games 欠落 / `valid:false` はログを出してスキップ                                                                                                        |
| ブック          | `opening-book-hard.json` の entries（白番 ply 3/5/7、`--min-ply=4` で ply 3 は落ちる）                                                                        | ≈ 5.9k、うちスイート起点と重複する数百〜千は dedup | 学習に入れる。歪みリスクは小（重みは全局面共通、ラベルは Rapfi 評価であってブック手ではない）。**ただしブック局面は hard 白番がブックで即答する局面なので、ブック局面の損失は成功指標にしない** |
| **序盤 prefix** | スイート v1（600）/ v2（382）の moves から **ply 4 / 5 / 6 の prefix 局面**を列挙（`--suite-prefix=<json>,...`）。黒番 ply 4/6・白番 ply 5                    | 最大 982×3、dedup 後 ≈ 2k                          | r3 教訓 (a) の「ply4-7 帯」の実体。**A2 の合格条件: ply 4-6 の行数（黒番・白番とも）が r2 の 624 を超えること**                                                                                 |

- quiet フィルタ（即五なし / hasVCF(200) なし / dedup）とラベル（Rapfi 100 ms、\|eval\|≤3000）は r2 と同じ。3 源とも**同じ関数**を通す（§2 の抽出）。
- 盤面キーはブック形式 `${boardToString(board)}|${stm}` に統一（コーパス独自の `b:/w:` 形式は廃止）。ブック行・prefix 行は `parseBoardKey` / 棋譜再生で盤面を作り、同じ `seen` で dedup。**dedup と回帰除外の照合は canonical key（対称正規化）で行う**（ブック key は canonical、棋譜は生の向きなので、生 key 照合では対称重複 118 局面がすり抜ける＝A1 レビュー実測）。行の `key` は生の向きのまま。r2 の dedup（生 key）とは意味論が変わる。
- `CorpusRow.source` に `kind: "kifu" | "book" | "prefix"` を追加。`groupKey` は kifu = `file#gameIdx`、book/prefix = 1 局面 1 グループ（親子局面が fold をまたぐのは許容）。
- 回帰ゲート局面（`regression-positions.ts` の `REGRESSION_POSITIONS` を lib に export）は `seen` に事前投入して学習から除外（テスト衛生。派生局面までは除外しない）。stats に `rejectedRegression` を出す。
- エンジン混在（06 は legacy 時代）は許容。源・月・ply 帯・手番の行数表を §4 に出す。
- 決定事項: ブックは再生成しない（Rapfi 誘導済み。asset の `weightGeneration` 乖離は許容）。ゲート起点局面（スイート 7 手）の学習含有も許容（leakage は軽微、記録する）。
- 配置: `bench-results/corpus/`（gitignore）。再生成手順は §5。旧 labeled JSONL とは key 形式が違うので resume は効かない（新規ディレクトリなので実害なし）。
- quiet フィルタの `hasVCF` は TS 版（局あたり 97 ms の主因）だが r2 と同一の事前登録定義なので本 PR では変えない。wasm VCF への差し替えはコーパス定義の変更として別途。
- prefix 行の大半は 09 棋譜の ply 4-6 と重なり kifu 側に吸収される（先勝ち）。合格条件は kind 横断の帯小計で見る。

## 2. 前段ツール A1（TDD、サブエージェント委任、PR は development 向け）

1. `scripts/types/prospectCorpus.ts` に `CorpusRow` を一本化（4 ファイルの重複定義を置換。`source.kind` 追加）。
2. `scripts/lib/prospectCorpus.ts`: `readBenchGames(path)`（両 JSON 形）、`tryEmitQuietPosition(...)`（quiet フィルタ + 特徴抽出 + dedup + emit を 1 関数に抽出。`sampleGame` / `sampleBook` / `samplePrefix` が共用）、`hasImmediateFive` / `extractFeatures` の移設、key 統一。
3. `prospect-corpus.ts`: `--book=<json>`、`--suite-prefix=<json,...> --prefix-plies=4,5,6`、入力 prefix `commit-bench-,weight-bench-`、`--out` 既定を `bench-results/corpus/`、統計に源別・ply 帯別・手番別の行数表。
4. `prospect-texel.ts`: `--include-source=<kind[,kind]>` / `--exclude-source` と、fold 平均 val loss の **源 kind × 月** 集計出力（§4 の診断と S1(i) の一般化テスト用）。
5. `scripts/lib/regressionPositions.ts` に `REGRESSION_POSITIONS` を切り出し（`regression-positions.ts` と prospect-corpus が import）。
6. `parseArg` 系の重複は触らない（範囲外）。

## 3. 学習・量子化・安い事前反証

- `prospect-texel.ts --k=5 --teacher=both --K=200`（rapfi 教師の fold 平均 val が主指標、outcome は参考）。`prospect-anchor.ts --min-support=100`、アンカー 6 セル据え置き。
- **事前反証 (i) 源横断の一般化テスト**: `--include-source=kifu/2026-06,kifu/2026-08 --holdout=kifu/2026-09,book,prefix` で fit し、holdout をセグメント別に評価。次に全源 fit の k-fold セグメント損失と比べる。差が小さければ新源は情報を持たない（同一コーパス内比較）。セレクタは kind と `kind/月` トークン。
- **事前反証 (ii) 差分の振り幅**: r2 → r4 の 34 セル差分（量子化後整数）を表にし、S4/S4' の勾配（±100 で ±10 Elo）と照らす。差分が希少セルだけで振り幅がその範囲内なら固定スクリーンを省略して「飽和」と結論してよい。
- J6 局面はブック未収録（実キーで確認済み）なので、回帰ゲートは hard 生探索経路の検証として機能する。

## 4. 診断（採否と独立に記録）

- 行数表: 源 kind × 月 × ply 帯（4-6 / 7 / 8-15 / 16-25 / 26+）× 手番。
- 損失: 源 kind × 月の baseline（r2 重み）と fit 後（同一コーパス内の相対比較のみ。r2 時点の 0.121 とは比較しない）。
- 差分表（r3 doc 形式）。BLACK_RISK / DOUBLE_THREE_WHITE の動きは S4/S4' の勾配（WHITE_TURN は下げ方向が微増）と整合するか。

## 5. 採否ゲートと実行計画

| #   | 作業                                                                                                                                                           | 手段                             | 所要                                                                             |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------- | -------------------------------------------------------------------------------- |
| A1  | §2 のツール変更                                                                                                                                                | サブエージェント（TDD）+ /review | 2〜3 h                                                                           |
| A2  | 抽出（48 JSON + ブック + prefix）→ ラベリング → fit → anchor。合格条件: ply 4-6 行数（kind 横断小計）> 624                                                     | 私（runbook）                    | 抽出 ≈ 25 分（09 分は 97 ms/局） / ラベル 40〜45 分（12〜15 万行） / fit ≈ 13 分 |
| A3  | 回帰ゲート（**FAIL なら即棄却**、r3 と同じボス条件。ブックで H6 を固定して再ゲートする案は取らない）、§3 事前反証、§4 診断                                     | 私                               | 30 min                                                                           |
| A4  | 固定スクリーン **前半のみ**（v2 382 局、注入、1.8 h）: 点推定 ≤ 0 または CI 上限 < +10 なら棄却。それ以外は後半を回さず時間ゲートへ                            | ベンチ                           | 1.8 h                                                                            |
| A5  | 時間ゲート `weight:bench`（時間モード）v1 1,200 局 jobs=5。合格 = 点推定 ≥ +10 かつ CI 下限 > −5                                                               | ベンチ                           | 6 h                                                                              |
| A6  | 焼き込みブランチ → `pnpm test` / `zig build test` / `test-golden`（期待値更新は理由をファイル内に記録）→ 振り返り判定の一致（参照棋譜 PRECISE）→ PR（/review） | 私                               | 2 h                                                                              |

- 「+10 未満でも損失改善なら採用」は不可（損失は製品指標でない。中立なら r2 据え置きのほうが安い）。
- weight-bench への 34 セル注入は量子化後の整数を `PROSPECT_<CAT>_WAIT/TURN` で渡す（CLI 長・read-back とも問題なし）。

## 6. 後段（r4 が飽和した場合、別メモ）

- 進入条件: A4/A5 で |Elo| < 10、かつ §3 (ii) の差分が微小 → 「現特徴集合で Texel は飽和」。
- 入力として先に `bench-results/horizon-flips-v2.jsonl`（142 局面、深さ 7 の根評価が 4 手以内に反転）で **static eval が見落とす形を分類**する（strength-screen §5 第 2 項）。
- 候補: Rapfi 古典 eval の「脅威状態 × 手番テーブル」相当を prospect 基底の上に載せる（Texel 適合前提。加算パッチ版は −124 で否定済み）、2 プライ平均。

## 7. 非目標

- 探索側の変更（P1 で完了）、下位難易度、振り返り側の変更、ブックの再生成。
- コーパスのリポジトリ管理（gitignore のまま `bench-results/corpus/` に置く）。

## 8. 結果（追記）

（未実施）

## 9. v1 → v2 の変更（レビュー反映）

- blocker: weight-bench 形 JSON のローダ追加（09 分 2,865 局の無音欠落を防止）／「ブック＝ply4-8」を訂正し、序盤帯はスイート prefix 列挙（ply 4/5/6）で供給、行数の合格条件を設定／「損失同点なら停止」を撤回し損失は診断専用に。
- should: key 統一・quiet フィルタ関数化・CorpusRow 一本化・源別集計・回帰局面 export・ブック/leakage の決定事項・規模と所要の実測値・max-per-game 24・ゲート順（固定は前半のみ、時間ゲート直行）・事前反証 2 種・J6 FAIL は即棄却・§6 進入条件と horizon-flips 分類・+181 の帰属訂正・集計基準の明記。
