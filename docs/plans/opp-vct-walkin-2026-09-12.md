# 相手の追い詰めに自ら入る手の検証（探索側レバー）— 設計メモ v2（2026-09-12〜13）

- 位置づけ: `strength-screen-2026-09-08.md` §8 の次段。eval 側（r4 Texel 再学習・r5 脅威状態テーブル）は飽和と判定したため、探索側に戻る。
- 発端: `horizon-flips-analysis-2026-09-11.md` §4-5「A 22 件は根で黒に VCT7〜13 があり白の探索は見えていない」。本メモはこの仮説を**先に数秒〜数十分の計測で検証**してから設計する（教訓: 2 h のベンチ前に「無制限で見つかるか」を測る）。
- v2: /review 3 観点（SOLID・パフォーマンス・ゼロベース）の指摘を反映。v1 からの主な変更は §5.2（予算の予約と両モードの総仕事量）、§5.3（margin 廃止）、§5.4（stats は append-only）、§9（実戦棋譜の走査＝序盤偏りの補正）。
- 計測スクリプトは scratchpad（`opp-vct-probe-measure.ts` / `-measure2.ts` / `opp-vct-verify-measure.ts` / `opp-vct-mode-budget.ts` / `opp-vct-control-measure.ts` / `walkin-sweep.ts`、未コミット）。数値は本メモに転記する。

## 0. 結論（先出し）

1. **「根で相手の追い詰めをプローブする」案は上積みなし**。A 22 件すべてで、実際に指された白の手が根の追い詰めを解消していた（lenient 深さ 8・100 万ノードでも残らない）。根の score が追い詰めを反映していなくても着手は正しかった。→ 撤回。
2. 本物の穴は **1 手先**にある。B / B-deep 73 件（準備局面で勝者に追い詰めが成立）の敗者の根で hard を再探索すると、**最善手の後に相手の追い詰め（lenient 深さ 5・20 万ノード）が残るものが 39/73**。そのうち探索が損を見えていない（root score > −1500）ものが **17 件**、上位 5 候補に安全な代替手があるものが **12 件**（うち 10 件は第 2 候補）。
3. 見えない理由は 2 つで、**予算の方が大きい**（39 件で数えた「1 手後の局面で追い詰めが見つかる件数」、深さ 4）:

   | モード \ ノード | 3k（現行プローブ） | 10k | 25k | 100k |
   | --------------- | -----------------: | --: | --: | ---: |
   | strict（現行）  |              **4** |   6 |  13 |   17 |
   | lenient         |                 10 |  13 |  21 |   32 |
   - 現行の防御ノード用プローブ（strict・3k）は 4/39 しか見ない。探索が損を見ていない 17 件では 0/17。
   - strict の「ノリ手ゲート」（相手の四のブロックが四でなければ手順崩壊とみなす、`vct.zig` `hasBreakingCounterFour`）は 17 件中 11 件を予算無制限でも棄却する。その 11 件のうち 7 件は勝者の探索が 99980（詰み確定）で反転している＝本物の負け筋を「幻」として捨てている。ゲートの精度（棄却のうち幻の割合）は最大 4/11。

4. 提案するレバーは 2 本（独立にスクリーニングできる）:
   - **S-L**: 防御ノードのプローブを strict → lenient（モジュール変数 `defensive_probe_mode` の既定値）。安いので先に固定スクリーン（2026-09-13 起動、§10）。
   - **V1**: 反復深化後に**最善手 1 手だけ**「置いた後に相手の追い詰めがあるか」を lenient・大予算で検証し、見つかれば上位 3 候補のうち安全な最上位に切り替える（`demotePlainFourIfNeeded` と同じ後処理の型）。予算は主探索から**予約**し、両モードの総仕事量を変えない。

5. 期待効果の上限（パフォーマンス観点の見積り）: 穴 17 件は plycheck v2 の 809 開局 × 4 手 ≈ 3,200 局面の 0.5%。100k で検出 15/17、安全な代替あり 12/17、上位 3 内 ≈ 9 件 → 実際に切り替わるのは局面の約 0.25%、1 局 30 手で **1 局の約 7% に 1 回**。全部が負け→非負けでも **+10〜20 Elo 級**。#163（+27.6）並みは期待しない。固定前半 382 局では保留域（点推定 ≥ +10 で CI が 0 を含む）に落ちる可能性が高いので、**保留なら後半を待たず時間モード 1,200 局で直接追試**（strength-screen §6.18 教訓 3）。

## 1. 計測 1: A 22 件（根に相手の追い詰め）

| 項目                                                          | 値                                                                       |
| ------------------------------------------------------------- | ------------------------------------------------------------------------ |
| 根の黒の追い詰めが見つかる最小ノード（lenient d4）            | VCF（四追い）8 件 = 3k 未満、VCT 14 件 = 25k×2 / 50k×3 / 100k×6 / 200k×3 |
| 同 strict d4                                                  | 14 件中 4 件は 400k でも見つからない（ノリ手ゲート）                     |
| 受け候補（手順セル ∪ 双方の脅威手）のうち追い詰めを解消する手 | 全件 2〜19 手ある                                                        |
| **実際に指された白の手が追い詰めを解消したか**                | **22/22（lenient d5 200k → d6 400k → d8 1M すべて「なし」）**            |
| 反転 3 手後の score                                           | 2,000〜5,000 帯が 18 件、勝ち級 4 件                                     |

→ 主探索の着手は正しく、根でのプローブは着手を変えない。反転は静的評価の振れが主体。

## 2. 計測 2: B / B-deep 73 件（1 手先の被追い詰め）

手順: 準備局面（勝者手番）から敗者の直前の手を外して敗者の根を作り、hard（深さ 7・10 s・100 万ノード・exact_top_k=5）で候補 5 手を取り、各候補を置いた後の勝者の追い詰めを lenient d5 200k で判定（真値）。JSON の `verify100k` 列は **strict** d5 100k で、lenient の代理ではない（脚注）。

| 項目                                                                    |                                                                       件数 |
| ----------------------------------------------------------------------- | -------------------------------------------------------------------------: |
| 敗者の根の再探索が実戦の手と一致                                        |                                                      38/73（上位 5 内 56） |
| 最善手の後に相手の追い詰めが残る                                        |                                                                      39/73 |
| うち root score ≤ −1500（探索が損を見ている＝代替手も負け筋の可能性大） |                                                      22（安全な代替 3 件） |
| **うち root score > −1500（探索が見ていない穴）**                       |                                **17（安全な代替 12 件、第 2 候補 10 件）** |
| 安全な代替との score 差（exact_top_k=5 の真値比較）                     | 0, 9, 18, 263, 373, 373, 507, 525, 713, 792, 1377, 1629, 1649, 2622, 99878 |

- 差分布は**真値**（exact_top_k=5）で取ったもの。製品 hard は `exact_top_k = 0` で、第 2 候補以降の score は aspiration 窓の fail-low **上限値**（真の差の下限）。したがって製品経路で bound に margin を当てても損失上限にはならない（§5.3 で margin を廃止）。
- 最善手が本物の追い詰めに入るなら真値は −FIVE なので、差の大小に関わらず切り替えが正しい。
- 準備局面の追い詰めが見つかる最小ノード（d5）: lenient は 3k 16 / 10k 12 / 25k 16 / 50k 10 / 100k 4 / 200k 3 / 400k 1 / 1M 2 / なし 9。strict は「なし」が 39/73。
- 計測 2 の敗者の実手が事前探索の即決経路（VCF・四受け等）だったかは記録していない。即決経路（探索手の 38%、strength-screen §8）は V1 の守備範囲外。

lenient d5 の所要（パフォーマンス観点の追加計測、無負荷・単一プロセス）:

| 集合                           | 予算 |  検出 | ms p50 / p90 / max       |
| ------------------------------ | ---: | ----: | ------------------------ |
| 穴 17 件                       |  25k | 10/17 | 73 / 84 / 97             |
| 同                             |  50k | 13/17 | 99 / 154 / 160           |
| 同                             | 100k | 15/17 | 98 / 320 / 328           |
| 追い詰めなし 34 件             | 100k |  0/34 | 276 / 361 / 410          |
| 対照群 100 局面（平均 8.0 石） | 100k | 0/100 | 0 / 273 / 602（平均 79） |

「見つからない」検証は静かな局面ではほぼ 0 ms（根に相手の脅威手が無い・三の受けや VCF で即終了）で、フル予算を使うのは尾だけ。1 手あたりの実コストは +80〜200 ms。

## 3. 原因の構造

1. **予算**: 主探索の防御ノード（相手手番）で走るプローブは VCT 3k（時間 25 ms）。#163 で 6k→3k にして +27.6 を得たとおり、**全ノードで走るプローブの上限は小さいほど良い**（20k で −60）。だが 1 手先（根の子）で長さ 7〜13 の追い詰めを見るには 25k〜100k 要る。全ノードで払えない予算は、**根の最善手 1 件にだけ**払う。
2. **モード**: strict のノリ手ゲート（dca749c、`vct-fp-asymmetric-2026-06-10.md`）は「相手に四が 1 本でもあり、そのブロックが四を作らなければ崩壊」と見なす。lenient は同じ場合に `checkSequenceBreaksByCF`（CF＋ブロックを置いた盤で残り手順が成立するか）まで見る。dca749c の「防御側の過剰棄却は低リスク（本当に被詰みなら受けは無い）」という前提は、**受けがある 1 手前**（本メモの 17 件）では成り立たない。過剰棄却が自ら追い詰めに入る手を許している。
   - dca749c 当時の Elo 検証結果は記録されていない（approach A の −88.7 は攻め側を締めた別案）。randomFactor 無効時代（[[project_bench_randomfactor_noop]]）なので当時の判断は現ベンチで再測が要る。
   - dca749c の幻は**実コーパスの中盤局面**で起きた。計測 1〜3 はすべて 7〜11 石の序盤帯（対照群平均 8.0 石）で、逆四が稀なため strict の棄却が本物に偏り、lenient の偽陽性が低く出ている可能性がある。→ §9 の実戦棋譜走査で中盤を測る。

## 4. レバー S-L: 防御ノードのプローブを lenient に

- 変更（ブランチ `screen/probe-lenient` f7b085f）: `minimax.zig` に `pub var defensive_probe_mode: vct_mod.ResilienceMode = .lenient;` を置き、`threatProbe` は `if (defensive) defensive_probe_mode else .lenient`。攻めノードは不変。採用時は既定値の変更で済み、同一 wasm で A/B も可能。
- 期待: 上表の 4 → 10（3k）。副作用 2 つ: (a) 幻の被詰み（dca749c の再現局面 G8 / −8864 → −99999）が戻り受け身手を選ぶ、(b) strict はノリ手ゲートで早期棄却するので lenient はプローブが高くつく（#163 の逆方向）。
- 判定は Elo 一本にしない。副指標: プローブ上限到達率・平均深さ（コストの分離）、**幻の被詰み回帰セット**（dca749c の再現局面 + §9 の走査で「lenient は追い詰めあり・strict はなし・指した側が負けていない」局面から数十件）で着手が変わった件数。
- 既知: `reviewExactTopK.wasm.test.ts` (d)「同値の 2 位以下が exactTopK=5 で分かれる」は特定局面の探索結果に依存する性質テストで、S-L で分かれなくなり落ちる（screen コミットはフック省略）。採用時は別局面に差し替える。Zig テスト 4,142 本・golden は緑（期待値不変）。
- 第 3 案 **S-G（strict ゲートの緩和）**: 棄却 11 件（本物 7）を材料に「ブロックが四でない」に加える条件（例: ブロックで相手に活三／ミセ／VCF が生じる場合だけ崩壊）を定義できる可能性がある。S-L の結果を見てから着手する（S-L が負なら S-G、正なら不要）。

## 5. レバー V1: 根の最善手の「自ら追い詰めに入る」検証

### 5.1 位置と責務

- `search.zig` `findBestMoveIterative` 末尾の後処理。`demotePlainFourIfNeeded` と共通部（仮置き＋bitboard 同期→`analyzeFourAndThree`→復元＝`isPlainFourMove`、`switchToCandidate`）を抽出し、demote と V1 を同一セクション（または `root_postprocess.zig`）に寄せる。`findVCTMoveWithLimiter` は内部で `bitboard.initFromCells` を呼ぶので、候補を戻したあと `bitboard.removeStone` で同期する（demote と同じ）。
- 事前探索の即決手（`immediate_move`）と唯一手の早期 return 経路には入れない。振り返り経路（aspiration_mode≠0）も demote と同様に通る（§5.6-6 で判定一致を確認）。
- **`finalizeStats(&ctx)` を後処理の後ろへ移す**（現状は demote の前に `final_result.stats` へコピーされる）。V1 の消費・skip はここで畳む。

### 5.2 予算（予約方式）

- **時間モード**: 主探索の `ctx.deadline` を `dynamic_time_limit − WALKIN_TIME_RESERVE` で立て、V1 は `TimeLimiter.untilDeadline(absolute_deadline).child(WALKIN_TIME_RESERVE, 0)` を親にする。hard は主探索が 10 s に張り付く手が 22〜24% あり、「絶対デッドラインとの min」だけでは**緊迫局面ほど V1 が走らない**。予約なら 1 手の合計時間は不変。
- **決定的モード**: 主探索 `max_nodes = N − WALKIN_NODES_RESERVE`、V1 の親予算 `WALKIN_NODES_RESERVE`。消費は `stats.nodes` に計上（事前探索・プローブと同じ規約）。**変種が追加予算を得ない**（N スイープでは +8% ≈ +5 Elo、+25% ≈ +15 Elo の交絡があり、期待効果と同じ桁）。demote の VCF（3k、非計上）は据え置き、差は明記。
- 親予算を K 回の検証で共有（子は親の残り全部）。「見つかった」は早く終わる（穴 17 件で p50 73〜98 ms）ので、代替候補の検証にも通常は予算が残る。最悪でも合計は予約額で頭打ち。
- 初期値: `WALKIN_NODES_RESERVE = 100_000`（1.2M の 8%）、`WALKIN_TIME_RESERVE = 400` ms（無負荷 100k「なし」p50 276 ms、jobs=5 負荷下は約 3 倍。時間側が固定側より弱いと固定で出た効果が時間ゲートで消える）。**P1 と同じ規則で較正**: ベンチ JSON の V1 `tripped` 率を両モードで一致させ、時間側を動かすか固定側を 50k に下げるかを到達率で決める。
- 定数は時間定数の隣（`search.zig`）、`BudgetPolicy.walkin_nodes`（TIME_MODE は 0、DETERMINISTIC は search 定数参照、`derive` テストに `> 0` を追加）。

### 5.3 手続き（margin なし）

```
if top_candidate_count < 2: return
if |result.score| >= WINNING_SCORE_THRESHOLD(2500): return   // 勝ち筋・負け確定は触らない
parent = 予約予算
checks = 0
置く(best); found = findVCTMoveWithLimiter(opponent, VCT_MAX_DEPTH=5, parent.child(残り全部), .lenient); 戻す; checks++
if parent が空で未判定: stats.walkin_skipped++; return
if !found: return                                   // 大半の手
for c in top_candidates[1..]:                       // 順位順、最大 WALKIN_MAX_CHECKS(3) 回まで
    if checks >= 3 or parent.exhausted(): break
    if isPlainFourMove(c): continue
    置く(c); ok = !findVCTMoveWithLimiter(opponent, ..., .lenient); 戻す; checks++
    if ok: switchToCandidate(result, c); stats.walkin_switches++; return
// 全候補が追い詰めに入る／予算切れ → 最善手のまま
```

- **margin は置かない**。製品経路の候補 score は bound（§2）で機能しないため、lenient 偽陽性の損失は**実戦介入率（§9）と screen** で縛る（§9.2 で偽陽性 0.4% を確認済み）。

#### 5.3a v3: 代替は「最善手を除外した根の再探索」で得る（V1c）

実装中に判明（2026-09-13）: 製品経路（exact_top_k=0）の第 2 候補以降は aspiration 窓の外で最初に切られた手が bound 順に並ぶだけで、真の第 2 候補ではない。局面 1（§5.6-1）は決定的 hard で候補 2〜5 が全部 −361 の同点かつ全部 walk-in、I6/J7/K8 は上位 5 に入らず、上記の候補ループでは切り替わらない（exact_top_k=5 なら F9 → I6 に切り替わる）。§2・§9.2 の「上位 3 に安全な代替 12/44」は exact_top_k=5 の真値順で測った数字であり、bound 順では再現しない。

→ 候補ループを廃止し、発火時（最善手が walk-in）は**最善手を除外集合に入れて根を反復深化で再探索**（深さ 2〜completed_depth−1、予算内で完了した最後の深さ）し、その最善手を VCT で検証、walk-in なら除外に加えて繰り返す（最大 `WALKIN_MAX_CHECKS = 3` 回）。根探索に除外集合（`SearchContext` の除外リスト、根の手ループでスキップ）を追加する。

予算（v3）:

- 決定的モード: VCT 検証は `WALKIN_NODES_RESERVE`（100k、主探索 N から予約、§5.2 のまま）。再探索は**発火時のみ**の追加予算 `WALKIN_RESEARCH_NODES = 150_000`／回で、`stats.nodes` と `walkin_nodes` に計上。発火は手の約 9%（§9.1）なので平均の追加は +1〜2% ≈ +1 Elo 未満の交絡（主探索からは予約しない。予約すると全手が 17% 浅くなり −10 Elo 級の税になる）。
- 時間モード: VCT 検証は `WALKIN_TIME_RESERVE`（400 ms、予約）。再探索は絶対デッドラインまでの残りと `WALKIN_RESEARCH_TIME = 1,000` ms の min。残り 100 ms 未満なら再探索せず `walkin_skipped`。10 s 張り付きの手（22〜24%）では再探索が走らないことを skip 率で観測し、時間ゲートで判定する。
- 二段判定（lenient で発見→strict で再検証し margin を階層化）は、strict が本物を 11/17 落とすため採らない。
- 相手の追い詰め探索は `findVCTSequenceInner` の仕様上、自分の最善手が活三を作る場合は VCF-only に落ちる（「四で活三を潰してから三で追う」相手の手順は見えない）。取りこぼし側の限界として明記。

### 5.4 統計・可観測性（append-only）

- `SearchStats` 末尾に `walkin_checks` / `walkin_switches` / `walkin_skipped` / `walkin_nodes` を追加し、stats_buffer は **append-only**（+68/+72/+76/+80、`STATS_FIELD_COUNT` 20、TS は `getStatsBufferLength() >= 84` で判定、`scripts/lib/wasmSearchStats.ts` の optional フィールド）。独立 export や features 新ビットは使わない（存在判定の SSoT を二重化しない。`budget.zig` の `searchFeatures` テストは値 3 固定）。
- commit-bench の `moveHistory[].stats` に載せ、screen の副指標: 介入率（switches／手）、skip 率（時間モード）、`tripped` 率（較正）、`walkin_nodes` の平均・最大。

### 5.5 やらないこと

- 根の全候補（20〜40 手）の検証。上位 3 手に限る。
- 深さ 1 の子ノードでプローブ予算を上げる（子は 20〜40 あり合計が主探索を食う。#163 の逆行）。
- 「相手の脅威手が 0 なら省略」ゲート: 対照群で 30% 発火するがその局面は元々ほぼ 0 ms で得が無く、穴 73 局面では 0% 発火。尾の制御はノード上限で足りる。

### 5.6 テスト（TDD、決定的モードで書く）

ネイティブテストは時計が 0 で時間予算が無制限になるため、`budget_mod.deterministic_mode = true; defer … = false;` で書く。時間モードは「予約で deadline が縮む」「予算切れで skip が数える」の分岐だけ確認。

1. 計測 2 の idx 22 相当（黒 H8 I8 J8 J6 / 白 G9 I9 G8 H7、黒番）: 現行は F9（置いた後に白 VCT7 `G7 G6 G11 G10 I7 F7 K7`）。V1 後は F9 以外（I6 / J7 / K8 のいずれか）。先に失敗テストを書く。同型で idx 23（黒 H8 H7 I6 H5 / 白 I8 G7 J7 H6、黒番、H9 → F8/J4）。
2. 「代替がすべて追い詰めに入る」局面では最善手を変えない（idx 5 相当）。
3. 最善手が四（活三を伴わない）の局面 1 件、最善手が活三を作る局面 1 件（VCF-only 限界の確認）、黒番で切り替え先が禁手でない（候補生成が保証）1 件。
4. 決定的モードで `stats.nodes` に検証分が計上され、主探索の `max_nodes` が予約分だけ減り、同一入力で棋譜が一致する（golden の期待値が変わる可能性: 変わったら理由をファイル内に記録）。
5. `regression-positions.ts` 全 PASS。
6. 振り返り（参照棋譜 FAST 13/14・PRECISE 14/14）の判定一致。V1 が最善手を差し替えると `result.score` が bound になり 150/400/2500 しきい値の差が縮む点に注意。振り返りでは差し替えず「相手の追い詰めあり」を flag で返す設計は、判定一致が崩れた場合の代替として残す。

## 6. リスク

| リスク                                             | 対処                                                                                           |
| -------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| lenient の偽陽性で良い手から劣る手に切り替える     | 介入率を §9（実戦・中盤込み）で測り screen の副指標に載せる。高ければ V1b（真値化＋差 ≤ 2000） |
| 時間モードで緊迫局面ほど検証が走らない             | 予約方式（§5.2）。skip 率を stats で観測                                                       |
| 時間と固定の対応が未較正で固定の効果が時間で消える | `tripped` 率一致で較正（P1 と同じ規則）                                                        |
| 決定的モードで変種が追加予算を得る                 | 予約方式で総仕事量を揃える                                                                     |
| 主探索が予約分（8%）浅くなる                       | V1 の真のコスト。screen に含めて測る                                                           |
| S-L と V1 の相互作用                               | 独立に screen し、両方候補なら合算ブランチで時間ゲート                                         |

## 7. 手順

1. 本メモ v1 を /review（3 観点、要修正 → v2 に反映済み）。
2. S-L: `screen/probe-lenient` f7b085f、固定前半 382 局（2026-09-13 00:03 起動、`bench-results/screen-sl-half1-2026-09-13.log`）。
3. 実戦棋譜の走査（§9）で中盤の介入率・偽陽性・敗局集中度を出す。
4. V1: §9 の結果で §5.3 の単純化（または V1b）を確定してからサブエージェントに実装委任（設計 §5、テスト §5.6）、diff は私が精査。
5. V1 固定前半 → 保留なら時間モード 1,200 局へ直行 → 判定。
6. 結果は §10 に追記。採用時は `strength-screen` メモ §8 と memory を更新。

## 8. 計測 3: 対照群の介入率（序盤帯）

plycheck v2 非反転（全 ply |score| ≤ 700、終局なし）の先頭 100 局面（平均 8.0 石）で、hard（深さ 7・10 s・100 万ノード・exact_top_k=5）の最善手を置いた後に相手の追い詰めが見つかるか。

| 項目                                                     |                                                    件数 |
| -------------------------------------------------------- | ------------------------------------------------------: |
| 最善手の後に lenient d5 100k で追い詰めあり（V1 が介入） |                                               **0/100** |
| 同 strict                                                |                                                   0/100 |
| 上位 5 候補のいずれかの後に lenient で追い詰めあり       | 5/100（いずれも最善手ではない＝主探索が既に避けている） |

序盤帯では介入率 0。**中盤には外挿しない**（§3-2）。

## 9. 計測 4: 実戦棋譜の全着手走査（中盤・終盤込み）

時間モード v1 1,200 局（`commit-bench-2026-09-11T12-17-33-759Z.json`、main 6db1b2a vs development dcf4888、双方 hard）の非開局手・非終局手について、「指した手の後に相手の追い詰め（lenient d5 100k）が残る」件数を数え、残る手については strict の有無・指した側の探索 score・対局結果・終局までの手数を記録する。

### 9.1 先頭 400 局（8,494 手、20 分）

| 項目                                                   |                           件数 |
| ------------------------------------------------------ | -----------------------------: |
| 指した手の後に相手の lenient 追い詰めあり（全 fire）   |         2,231 / 8,494（26.3%） |
| 同・指した側の対局結果 負け／勝ち／引分                |               1,886 / 315 / 30 |
| **V1 の介入対象（\|score\| < 2500 の fire）**          |           **758（手の 8.9%）** |
| 同・結果 負け／勝ち／引分                              |              **725 / 15 / 18** |
| 同・strict でも追い詰めあり                            |                     406（54%） |
| 同・石数 ≤10 / ≤15 / ≤20 / ≤30 / ≤40 / それ以上        | 41 / 186 / 166 / 238 / 72 / 55 |
| 敗者が最初に穴に入った手（対局ごと）                   |             331 局（400 局中） |
| 同・終局までの手数 ≤2 / ≤4 / ≤6 / ≤10 / ≤20 / それ以上 |     7 / 21 / 57 / 148 / 95 / 3 |
| 同・strict でも追い詰めあり                            |                     104（31%） |

読み:

- 介入対象は序盤帯の 0/100 と違い、実戦では手の 9%。ただし**対象の 96% は指した側が負けた対局の手**で、勝った対局の手は 2%（15 件、§9.2 で偽陽性か確認）。lenient の「相手の追い詰めあり」は中盤でも負けの予測として精度が高い。
- 敗局 331/400 で、敗者は score が ±2500 の内側のまま（＝損を見ていない）相手の追い詰めに入っており、その最初の手は終局の 6〜20 手前が主体、石数 10〜20 が半分。**探索が見えていない被追い詰めは例外でなく敗局の常態**。ここで安全な代替があるかが V1 の効果の上限（§9.2）。
- 最初の穴の 69% は lenient のみ（strict は棄却）。S-L の対象でもある。
- 介入率 9% は V1 のコスト見積りに影響: 「見つかった」後の代替検証（最大 2 件）が手の 9% で走る。予約方式（§5.2）なので合計は予約額で頭打ち。

### 9.2 最初の穴 60 件の再探索と、勝った対局の fire 15 件の精査（`walkin-sweep-alt.ts`、20 分）

(a) 敗者の「最初の穴」から 5 件おきに 60 件。その手の直前局面を hard（深さ 7・10 s・100 万ノード・exact_top_k=5）で再探索し、候補 5 手それぞれの後の相手の追い詰め（lenient d5 100k）を判定。

| 項目                                                         |                  件数 |
| ------------------------------------------------------------ | --------------------: |
| 再探索の最善手が実戦の手と一致                               | 39/60（上位 5 内 50） |
| 再探索の最善手の後に相手の追い詰めが残る                     |                 44/60 |
| 同・上位 3 候補に安全な代替あり（第 2 候補 11、第 3 候補 1） |             **12/44** |
| 同・上位 5 候補に安全な代替なし                              | 31/44（すでに負け筋） |
| 再探索の最善手は追い詰めを避けた（時間モードの非決定性）     |                 16/60 |

→ 実戦の「最初の穴」で V1 が切り替えられるのは約 4 分の 1（12/44）。残りは 5 候補すべてが追い詰めに入る＝その 1 手前で既に負けている（V1 の守備範囲外。より前の手で穴に入っているか、追い詰め以外の負け）。敗局 331/400 × 27% ≈ 対局の 2 割で敗者の最初の穴が回避可能、が上限。序盤 17 件の見積り（§0-5、対局の 7%）より大きい。

(b) 介入対象のうち指した側が勝った 15 件（偽陽性候補）。

| 項目                                                        |      件数 |
| ----------------------------------------------------------- | --------: |
| lenient d6 400k でも追い詰めあり                            |     15/15 |
| strict d5 100k でも追い詰めあり                             |      3/15 |
| **相手側を hard で探索すると score ≥ 9000（本物の勝ち筋）** | **12/15** |
| 相手の実手が探索の最善手と違う（勝ち筋を取りこぼした）      |      3/15 |

→ 15 件のうち 12 件は本物の追い詰めで、相手がその後に取りこぼしただけ（V1 が切り替えても損はない）。残り 3 件（相手 score 0〜1,470）が偽陽性または相手側の探索の取りこぼしの可能性で、介入対象 758 件の **0.4%**。中盤でも lenient の偽陽性は実質無視できる。→ §5.3 の「margin なし」で進める。V1b（真値化）は不要。

### 9.3 判断

- V1 は実装に進む（§7-4）。期待効果の上限は §0-5 を上方修正: 敗局の約 2 割で最初の穴を回避できる。両者が持つので自己対局の Elo は「相手より先に穴に入らない」差として出る。
- S-L は固定スクリーンの結果（§10）で判断。最初の穴の 69% が lenient のみなので、S-L が正なら V1 と合算する。

## 10. 結果

### 10.1 V1c 実装（2026-09-13、ブランチ `feat/walkin-verify`、24681b9 → bcfddd0 → 1e7064a）

- サブエージェント実装、diff は私が精査。§5 v3 どおり: `avoidWalkInIfNeeded`（§5.3a）、`SearchContext.root_excluded`（225 ビット、根の手ループのみ）、`researchExcluded`（除外集合付き反復深化 2〜completed_depth−1、固定幅 aspiration＋全窓フォールバック）、`isPlainFourMove` / `switchToCandidate` / `movePvToFront` の抽出、`finalizeStats` を後処理の後ろへ、stats +68〜+80（`STATS_FIELD_COUNT` 21 = 84 バイト）、`BudgetPolicy.walkin_nodes`、テスト `zig/src/search_walkin_test.zig`（10 本、test-golden に登録）。
- 実装側の判断（メモに無かった点）: 検証の時間予算は検証ごとに 400 ms を立て直す（親 1 本だと間の再探索で食われる）／時間予約は `min(400 ms, 動的時間の 20%)`（easy 1 s で主探索が消えないように）／`max_nodes ≤ 予約額` なら予約しない／再探索は両モードで `stats.nodes + 150k` に延長／`completed_depth < 3` は再探索せず skip にも数えない／`walkin_skipped` は「上限に当たって『なし』（tripped）」も含む（較正用）。
- 決定的 hard（深さ 7・1.2M）: 局面 1 F9 → **J7**（checks 3 / switches 1 / walkin_nodes 222k、総 371k）、局面 2 H9 → **I5**（checks 2 / switches 1 / 178k）、全候補 walk-in 局面は G7 のまま（checks 3 / switches 0）。
- `zig build test` 緑、`test-golden` 緑（**golden A/B 期待値不変**: 時間モードは V1 を nodes に計上せず時計 0 では予約が効かない）、vitest 2,288/2,289（1 件は負荷起因の 5 s タイムアウト、単独 3.7 s 緑）、`pnpm check-fix` 緑、`regression-positions.ts` 全 PASS。
- 振り返り判定（参照棋譜 白番 29 手、PRECISE、development 643fcda のベンチ worktree と V1c worktree、S-L ベンチと並走）: **14/14 一致**（excellent 6 / inaccuracy 1 / mistake 2 / blunder 5 が同一、被追い詰め・被 VCF の検出も同一）。差は手 10 の最善手（E9 −678 → I9 −732、実手 E9 の評価は両方 −678）のみ。所要 同等。

### 10.2 S-L 固定前半（2026-09-13、A=643fcda / B=f7b085f lenient、v2 前半 382 局、1.2M/3k、jobs=7、120 分、V1c 実装・走査と並走）

- **A 視点 0 [−23.3, +23.3]（191 ペア、ll=19 ld=8 dd=138 wd=6 ww=20、1-1 ペア 72%）＝ lenient は ±0**。JSON `bench-results/commit-bench-2026-09-12T17-01-56-931Z.json`。
- 副指標（B lenient 側）: プローブ上限到達率 20.6% → **25.4%**、プローブ消費 740 → 887 ノード/回（+20%）、プローブの総ノード比 68.5% → 72.1%。lenient は同じ 3k でも高くつく（パフォーマンス観点の予想どおり）。効果（本物の負け筋を 3k で 4→10 件見る）とコスト（主探索が浅くなる）が相殺した形。
- 判定: 規約上は ≥0 で後半に進めるが、点推定 0 では 764 局で +10 に届く見込みが薄く、V1c のスクリーンを優先する。**S-L は保留（低優先）**。幻の被詰み回帰セットは作っていない（採用候補にならなかったため）。S-G（strict ゲート緩和）は V1c の結果を見てから。

### 10.3 V1c 固定前半（2026-09-13、A=643fcda / B=1e7064a、v2 前半 382 局、1.2M（B は主探索 1.1M + 予約 100k + 発火時 150k/回）、jobs=7、102 分）

- **A 視点 −16.4 [−38.6, +5.7] ＝ V1c は +16.4 [−5.7, +38.6]**（191 ペア、ll=16 ld=11 dd=150 wd=3 ww=11、1-1 ペア 79%）。JSON `bench-results/commit-bench-2026-09-12T18-44-44-947Z.json`。
- 規約: 点推定 ≥ +10 で CI が 0 を含む＝**保留** → §0-5 の方針どおり後半を待たず**時間モード v1 1,200 局へ直行**（A=643fcda / B=1e7064a、jobs=5、`bench-results/gate1-v1c-time-2026-09-13.log`）。合格 = 点推定 ≥ +10 かつ CI 下限 > −5。
- 注意: このランの JSON には `walkin*` 統計が載っていない。commit-bench の stats リーダー（`scripts/lib/wasmSearchStats.ts`）は**メインリポジトリ側**の版が使われ、当時のブランチ（screen/probe-lenient）に新フィールドが無かったため。時間ゲートはメインリポジトリを `feat/walkin-verify` に切り替えて起動したので介入率・skip 率・`tripped` 率が取れる。
- 総ノード（B）は 1 手平均 428k vs A 443k（−3%）。1.2M 上限に張り付く手だけ主探索が 100k 減り、発火手で +150k/回が乗る。

### 10.4 V1c 時間ゲート（2026-09-13、A=643fcda / B=1e7064a、v1 1,200 局、jobs=5、5.5 h、nohup）

- **A 視点 −15.1 [−27.5, −2.6] ＝ V1c は +15.1 [+2.6, +27.5]**（600 ペア、ll=47 ld=4 dd=523 wd=6 ww=20、1-1 ペア 87%、中断 0）。JSON `bench-results/commit-bench-2026-09-13T00-18-40-055Z.json`。
- **合格**（点推定 ≥ +10 かつ CI 下限 > −5）。固定前半 +16.4 と一致し、固定→時間の転移も成立。
- V1c 側の統計（1 手あたり、B 15,074 手）:

  | 指標                                       |        A |                             B |
  | ------------------------------------------ | -------: | ----------------------------: |
  | 1 手平均時間                               | 3,300 ms |                      3,289 ms |
  | 10 s 張り付き（≥ 9.9 s）                   |    14.3% |                      **2.9%** |
  | 平均深さ（全着手、即決手込み）             |     4.34 |                          4.27 |
  | 検証回数／手（`walkin_checks`）            |        – |                          0.51 |
  | 発火（checks ≥ 2＝最善手が walk-in）       |        – |                          2.2% |
  | **切り替え（`walkin_switches`）**          |        – | **0.90%**（約 110 手に 1 回） |
  | 判定不能（`walkin_skipped`、旧定義＝予約枯渇・tripped・再探索の時間不足の合算。33be0dd 以降は最善手検証の判定不能のみ） |        – |                         10.3% |
  | V1c 消費ノード／手                         |        – |                         11.7k |
  | プローブ上限到達率                         |    19.8% |                         19.7% |

- 読み: 発火は実戦走査（§9.1、8.9%）より低い 2.2%。差は (a) 走査は「実手の後」で全手を数えたが V1c は `|score| < 2500`・非即決手のみ、(b) 400 ms（負荷下）では 100k 相当に届かず tripped が 10.3% ある、(c) 検証を受けた手が 51%（残りは即決手・勝ち負け確定）。切り替え 0.9%/手 で +15 Elo は §9.2 の「敗局の 2 割で最初の穴を回避」と桁が合う。
- 10 s 張り付きが 14.3% → 2.9% に落ちたのは主探索の締切が 400 ms 手前に立つため（合計時間は不変）。
- 較正（§5.2）: 時間側の tripped 率 10.3% に対し固定 100k の到達率は未計測（固定前半の JSON に統計が無い）。採用後の follow-up として、次の固定スクリーンで B 側の `walkin_skipped` を読み、時間 400 ms ↔ 固定 100k の対応を確認する。

### 10.5 判定

- **V1c 採用**。PR `feat/walkin-verify` → development（3 観点レビュー後にマージ）。
- follow-up: (1) 時間 400 ms ↔ 固定 100k の到達率較正、(2) 除外再探索と TT 共有の振り返り PV 影響、(3) S-L は保留・S-G（strict ゲート緩和）は棄却 11 件の内訳から条件を定義できれば別メモで、(4) 実戦走査 §9.1 の「最初の穴で 5 候補すべて walk-in」31/44 局面は V1c の範囲外（1 手前で既に負け）＝次の穴はそこ（2 手先の被追い詰め、または敗者の探索が損を見ていない理由）。

### 10.6 実装レビュー（3 観点、Opus、2026-09-13）と対応

3 観点とも要修正。指摘を「hard の対局経路の挙動を変えない修正（PR に入れる）」と「探索挙動が変わる改良（再ベンチが要る follow-up）」に分けた。

PR に入れる（採用済みの時間ゲート結果を無効にしない範囲）:

1. 適用範囲のゲート: `params.max_depth < 3`（beginner / easy）は予約も検証もしない（切り替えは原理的に起きないのに主探索の 20% を失っていた）。`completed_depth < 3` の判定を最初の検証より前へ。
2. 振り返り経路（aspiration_mode≠0 / exact_top_k>0）では走らせない: 切り替えで `result.position/score` と `top_candidates`/`exact_mask` の整合が壊れ、`fullEval.ts` の `playedIsBest` / `resolvePlayedScore` が「穴に入った手を最善」と判定し得る。除外再探索の TT 上書きが候補 PV にも影響する。振り返りの 14/14 一致は経験的証拠であり不変条件違反は残っていた。
3. 小予算の決定的モード（`max_nodes ≤ 予約額`）は検証を走らせない（主探索 50k に対して検証 100k が走り N スイープの較正を壊す）。
4. stats の分離: `walkin_fired` / `walkin_vct_nodes` を追加（+84/+88、92 バイト）、`walkin_skipped` は最善手の検証が判定不能のみ、`walkin_nodes` は除外再探索の消費のみ（時間モードで `stats.nodes − walkin_nodes` が主探索ノードになるように）。
5. ループ制御をローカル変数に、`verifyWalkIn` を純粋関数に、`.stats = undefined` をやめる、切り替え先の score 下限ガード（負け確定への切り替えを禁止）、既知の逸脱（時間モードでも再探索が `max_nodes` を最大 +300k 超える）のコメント明記。

follow-up（別メモ・要スクリーン）:

- 除外再探索と主探索の TT 共有: 浅い exact が深い exact を無条件に上書き（`tt.zig` の置換規則）。次の手の再利用を失うだけで誤値は使わない（probe は深さを確認）。対処案は再探索中の `newGeneration()` か store 抑止。
- `research_depth = completed_depth`（現状 −1）の変種。根の子は主探索で埋まっているので追加コストは小さく、切り替え先の質が上がる。
- 切り替え先の非生産的四フィルタ（§5.3 v1 にあったが v3 で落ちた）。demote が降格した四に V1c が戻す経路がある。
- `root_excluded` を `SearchContext` から外し、MoveList のコピーをフィルタして渡す（minimax の差分がゼロになる。`moves.len == 1` の早期経路の扱いを決める）。
- 時間モードの再探索ノード天井（`params.max_nodes` を超えない）と、時間 400 ms ↔ 固定 100k の到達率較正（`walkin_skipped` / `walkin_fired` から）。
- 振り返りに「相手の追い詰めあり」を flag で返す設計（§5.6-6）。
- aspiration 1 深さ分の共通ヘルパー化（主ループと `researchExcluded` の SSoT）。

- 残る観点（採否後の follow-up）: 除外再探索が主探索の TT を共有するため、発火した手では根の子局面の TT エントリが浅い深さで上書きされうる（PV 抽出は最善手起点なので着手には無関係、振り返りの候補 PV 表示に影響しうる）。
